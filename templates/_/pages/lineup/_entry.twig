{% extends "_/layouts/base"%}
{% block content %}
    <div class="container">
        <h1>{{entry.title}}</h1>
        <p>{{entry.time}}</p>
        <p>{{entry.stage}}</p>
        IDK ANYMORE
        KOM ON
    </div>
{% endfor %}